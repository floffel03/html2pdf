#!/bin/bash

which phentomjs && echo 'Phantomjs is installed. g2g!' || echo '-- ⚠ ATTENTION ⚠ --' && echo 'In order for this to work, you need to install phantomjs.' && echo 'See http://phantomjs.org/download.html for install instructions.'
